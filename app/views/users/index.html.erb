<%if user_signed_in?%>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>

      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
    <p><%= link_to 'Debes loggearte aquí para ver los usuarios', new_user_session_path, class: 'btn btn-success' %></p>
<% end %>




<p><%= link_to 'Nuevo Usuario', new_user_registration_path, class: 'btn btn-warning' %></p>

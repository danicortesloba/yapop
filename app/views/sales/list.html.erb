<div class="row">
  <div class="col-md-6">
    <h1>Vendidos</h1>
    <% @products.each do |product| %>
      <%if product.sold == true %>
        <ul>
          <li><%= product.name%></li>
        </ul>
      <%end%>
    <%end%>
  <%= link_to 'Back', products_path, class: 'btn btn-warning' %>
